
/*import IPlanoEntregasService from './IServices/IPlanoEntregasService';

describe('PlanoEntregasServiceIntegracaoContentores', () => {

    it('Teste plano frota planeamento', async () => {
        // public async getFrotaPlaneamento(dia: number, mes: number, ano: number) {
        const PlanoEntregasService: IPlanoEntregasService = require('./planoEntregasService').default;

        PlanoEntregasService.getFrotaPlaneamento(6, 12, 2022).then((result) => {
            expect(result).not.toBeNull();
            expect(result).not.toBeUndefined();
            expect(result.length).toBeGreaterThan(0);
            expect(result[0].matricula).not.toBeNull();
            expect(result[0].matricula).not.toBeUndefined();
        });
    });

    it('Teste plano frota simulada', async () => {
        // public async getFrotaSimulada(dia: number, mes: number, ano: number) {
        const PlanoEntregasService: IPlanoEntregasService = require('./planoEntregasService').default;

        PlanoEntregasService.getFrotaSimulada(6, 12, 2022).then((result) => {
            expect(result).not.toBeNull();
            expect(result).not.toBeUndefined();
            expect(result.length).toBeGreaterThan(0);
            expect(result[0].matricula).not.toBeNull();
            expect(result[0].matricula).not.toBeUndefined();
        });
    });

    it('Teste plano frota planeamento sem entregas', async () => {
        // public async getFrotaPlaneamento(dia: number, mes: number, ano: number) {
        const PlanoEntregasService: IPlanoEntregasService = require('./planoEntregasService').default;

        PlanoEntregasService.getFrotaPlaneamento(5, 09, 2022).then((result) => {
            expect(result).not.toBeNull();
            expect(result).not.toBeUndefined();
            expect(result.length).toBe(0);
        });
    });
});
*/
